const std = @import("std");
const print = std.debug.print;

test "expect twoSum to return 1" {
    try std.testing.expect(twoSum(2) == 3);
}
// Working on it!
pub fn twoSum(nums: []const i32,  target: i32) [2]const i32 {
  var result: [2]const i32;

  return result;
}

pub fn main() void {
    print("hello\n", .{});
}
